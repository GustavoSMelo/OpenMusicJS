const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'config', 'sql', 'config.js'),
    'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'),
};
