import React from 'react';

function Player() {
    <audio controls>
        <source
            src={`http://localhost:3333/music/${props.musicname}`}
            type="audio/mpeg"
        />
    </audio>;
}

export default Player;
